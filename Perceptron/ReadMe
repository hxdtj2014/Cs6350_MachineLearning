The scripts of running the file are at then end of each file.
Just change the file directory (files in the perceptron directory) in the file reading line. The code will automatically generate the results
